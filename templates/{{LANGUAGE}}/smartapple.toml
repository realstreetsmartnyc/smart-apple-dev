# {{NAME}} Project - {{LANGUAGE}}
# {{PLATFORM}} Application Template
# Generated by smart-apple-dev

# Configuration
PROJECT_NAME = "{{NAME}}"
BUNDLE_ID = "{{BUNDLE_ID}}"
VERSION = "0.1.0"
MIN_OS = "{{MIN_OS}}"
TARGET = "{{TARGET}}"
LANGUAGE = "{{LANGUAGE}}"

# Build Settings
BUILD_DIR = "build"
OUTPUT_DIR = "dist"
SOURCE_DIR = "Sources"
TEST_DIR = "Tests"
ASSETS_DIR = "Assets.xcassets"

# Dependencies
DEPENDENCIES = []

# Platform-specific settings
{% if PLATFORM == "ios" %}
SUPPORTED_DEVICES = ["iphone", "ipad"]
REQUIRED_CAPABILITIES = []
{% elif PLATFORM == "macos" %}
SUPPORTED_DEVICES = ["mac"]
REQUIRED_CAPABILITIES = []
{% elif PLATFORM == "tvos" %}
SUPPORTED_DEVICES = ["apple-tv"]
REQUIRED_CAPABILITIES = []
{% elif PLATFORM == "watchos" %}
SUPPORTED_DEVICES = ["apple-watch"]
REQUIRED_CAPABILITIES = []
{% elif PLATFORM == "visionos" %}
SUPPORTED_DEVICES = ["vision-pro"]
REQUIRED_CAPABILITIES = []
{% endif %}

# Code Signing
CODE_SIGNING = {
    "team_id": "{{TEAM_ID}}",
    "provisioning_profile": "{{PROVISIONING_PROFILE}}",
    "signing_style": "automatic",
}

# CI/CD Configuration
CI = {
    "provider": "{{CI_PROVIDER}}",
    "test_on_simulator": true,
    "auto_deploy": false,
}

# Testing
TESTING = {
    "framework": "xctest",
    "coverage": true,
    "unit_tests": true,
    "ui_tests": true,
    "performance_tests": true,
}

# Analytics (Firebase)
FIREBASE = {
    "analytics": true,
    "crashlytics": true,
    "remote_config": true,
    "messaging": true,
}